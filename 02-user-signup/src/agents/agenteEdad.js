// TODO: implementar
// - input es el objeto usuario con campo `fechaNac` (formato YYYY-MM-DD)
// - calcular edad a partir de la fecha
// - AGREGAR el campo `edad` al objeto
// - validar que sea mayor o igual a 18
// - si pasa: { ok: true, valor: { ...input, edad: edadCalculada } }
// - si falla: { ok: false, agente: 'agenteEdad', error: 'mensaje claro' }

export function agenteEdad(input) {
  return { ok: false, agente: 'agenteEdad', error: 'Sin implementar' };
}
