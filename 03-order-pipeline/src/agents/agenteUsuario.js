import { usuariosValidos } from '../data/mockData.js';

// TODO: implementar
// - input es el pedido completo
// - buscar pedido.usuario.id dentro de usuariosValidos
// - si existe: { ok: true, valor: input }
// - si no existe: { ok: false, agente: 'agenteUsuario', error: 'Usuario no registrado' }

export function agenteUsuario(input) {
  return { ok: false, agente: 'agenteUsuario', error: 'Sin implementar' };
}
